@model Web.Models.ChartsModel
@{
    ViewBag.Title = "Графіки";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section headerscripts
{
    @Scripts.Render("~/bundles/highchart")
    <link rel="stylesheet" href="/Content/themes/custom/jquery-ui.css">
    <style type="text/css">
        .ui-tabs .ui-tabs-panel {
            display: flex;
        }
        /*tspan, text {
            font-size: medium;
        }*/
        #tab1, #tab2, #tab3, #tab4 {
            padding: 10px;
        }
    </style>
}
<h2>@ViewBag.Title</h2>
@*<img src='@Url.Action("CountSalesProduct")'/>
    <img src='@Url.Action("TotalPriceSalesProduct")'/>*@


@*<img src="CountSalesProduct.cshtml" alt="" />*@






<div id="tabs">
    <ul>
        <li><a href="#tab1">Кількість товарів</a>
        <li><a href="#tab2">Вартість товарів</a>
        <li><a href="#tab3">По типу доставки</a>
        <li><a href="#tab4">По формi оплати</a>
        <li><a href="#tab5">Трансакційні витрати</a>
</ul>
    <div id="tab1">
        @if (Model.Chart1 != null)
        {<div class="col-md-12">@Model.Chart1</div>}
        else {<h2>Немає данних</h2>}
    </div>
    <div id="tab2">
        @if (Model.Chart2 != null)
        {<div class="col-md-12">@Model.Chart2</div>}
        else {<h2>Немає данних</h2>}
    </div>
    <div id="tab3">
        @if (Model.Chart3 != null)
        {<div class="col-md-12">@Model.Chart3</div>}
        else {<h2>Немає данних</h2>}
    </div>
    <div id="tab4">
        @if (Model.Chart4 != null)
        {<div class="col-md-12">@Model.Chart4</div>}
        else
        {<h2>Немає данних</h2>}
    </div>
    <div id="tab5">
        @if (Model.Chart5 != null)
        {<div class="col-md-12">@Model.Chart5</div>}
        else
        {<h2>Немає данних</h2>}
    </div>
</div>

@section scripts
{
    <script>

        $(function () {
            $('#tabs').tabs({
                show: function (event, ui) {
                    var $activeTab = $('#tabs').tabs('option', 'active');
                    if ($activeTab == 1) {

                    }
                    if ($activeTab == 2) {

                    }
                    if ($activeTab == 3) {

                    }

                }
            });
        });
    </script>
}
